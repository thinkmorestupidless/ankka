# A tag `v*` publishes the six application-facing modules to Maven Central through the Sonatype
# Central Portal, signed. Everything else in the build carries `publish / skip`.
#
# Waits on one thing outside this repository: the `com.thinkmorestupidless` namespace must be
# claimed on the Central Portal (https://central.sonatype.com) — a one-time action by the
# maintainer — and the four secrets below set on the repository:
#   PGP_SECRET          gpg --armor --export-secret-keys <id> | base64
#   PGP_PASSPHRASE      the key's passphrase
#   SONATYPE_USERNAME   the user-token *username* from the portal (not the account name)
#   SONATYPE_PASSWORD   the user-token *password*
# Until then, the same path is proven locally: `sbt -Dnakka.release.local=/tmp/nakka-repo
# publishSigned` with a throwaway key (specs/006-external-applications/quickstart.md, Tier 4).
#
# Commits to main publish `-SNAPSHOT`s; only a tag is a release (sbt-dynver derives both).
name: release
on:
  push:
    branches: [main]
    tags: ["v*"]
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # dynver needs the tags
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: sbt
      - uses: sbt/setup-sbt@v1
      - run: sbt templateVersion ci-release
        env:
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
  template:
    # The template as its own repository, so `sbt new thinkmorestupidless/nakka.g8` works: sbt's
    # Giter8 resolver accepts only `owner/repo.g8` (or a `.g8` file path), never a subdirectory of
    # another repository. Pushed from nakka.g8/ on every tag, after the artifacts it names exist.
    needs: publish
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          git config user.name "nakka release"
          git config user.email "release@nakka.invalid"
          git subtree split --prefix nakka.g8 -b nakka-g8
          git push --force "https://x-access-token:${{ secrets.TEMPLATE_REPO_TOKEN }}@github.com/thinkmorestupidless/nakka.g8.git" nakka-g8:main
          git tag -f "${GITHUB_REF_NAME}" nakka-g8
          git push --force "https://x-access-token:${{ secrets.TEMPLATE_REPO_TOKEN }}@github.com/thinkmorestupidless/nakka.g8.git" "${GITHUB_REF_NAME}"
